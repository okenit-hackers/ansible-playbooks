---
- name: Установка dashboard
 hosts: '{{ hosts }}'
 become: true

 pre_tasks:
 - name: Загрузка /tmp/dashboard.tar.gz
  get_url:
  url: http://192.168.5.119:8000/dashboard.tar.gz
  dest: /tmp/dashboard.tar.gz
  delegate_to: ansible_host

 roles:
 - ../roles/dashboard